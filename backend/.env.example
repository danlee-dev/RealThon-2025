# Backend Environment Variables Template

# Database
DATABASE_URL=sqlite:///./interview_coach.db

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# GitHub API (Optional - for higher rate limits)
GITHUB_TOKEN=your_github_token_here

# Authentication
# SECRET_KEY: Used for JWT token signing (HS256 algorithm)
# Generate a secure random key: openssl rand -hex 32
# Token expiration: 7 days (hardcoded in auth.py)
SECRET_KEY=your_secret_key_change_this_in_production
# JWT Secret

# ──────────────────────────────────────────────────────────
# Voice Interview System (STT → LLM → TTS)
# ──────────────────────────────────────────────────────────

# 🔴 Phase 1: MVP (로컬 Whisper + 로컬 Melo)
WHISPER_LOCAL_MODEL=base
WHISPER_LOCAL_DEVICE=cpu
MELO_TTS_BASE_URL=http://localhost:8001

# ✅ Phase 2: A6000 서버 (모두 로컬 GPU)
USE_A6000_MODELS=false
A6000_STT_URL=http://a6000-server:8002
A6000_LLM_URL=http://a6000-server:8003
A6000_TTS_URL=http://a6000-server:8004
